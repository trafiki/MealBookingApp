// Deconstruct Router from express
// instead of import express from 'express' | const router = express.Router();
import { Router } from 'express';
// Controller
import mealController from '..'

// An instance of router
const router = Router();

// '/api/v1/meals/'
router.get('/', )

export default router;
